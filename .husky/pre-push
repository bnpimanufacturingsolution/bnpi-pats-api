#!/bin/sh

# # Get the current branch name
# branch=$(git rev-parse --abbrev-ref HEAD)

# # Protected branches that cannot be pushed to directly
# protected_branches="develop main master"

# for protected in $protected_branches; do
#     if [ "$branch" = "$protected" ]; then
#         echo "❌ Direct push to '$branch' is not allowed."
#         echo "   Please create a feature branch and open a pull request."
#         exit 1
#     fi
# done

# # Also check if trying to push TO a protected branch
# while read local_ref local_sha remote_ref remote_sha; do
#     if echo "$remote_ref" | grep -qE "refs/heads/(develop|main|master)$"; then
#         remote_branch=$(echo "$remote_ref" | sed 's|refs/heads/||')
#         echo "❌ Push to protected branch '$remote_branch' is not allowed."
#         echo "   Please create a pull request instead."
#         exit 1
#     fi
# done

echo "✅ Push allowed"
